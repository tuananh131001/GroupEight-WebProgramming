<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Product List</title>
</head>
<body>
    <h1>My Product List</h1>
    <ul>
        <% products.forEach(product => { %>
            <li>
                <h2><%= product.name %></h2>
                <p>Price: $<%= product.price %></p>
                <img src="/public/images/<%= product.image%>" alt="<%= product.name %>">
                <form action="/products/vendors-only/my-products/<%= product.id %>/delete" method="POST" >
                <input  type="submit" class="btn btn-danger btn-lg px-4"></input>

                </form>
            </li>
        <% }); %>
    </ul>
</body>
</html>