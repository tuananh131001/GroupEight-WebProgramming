<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product List</title>
</head>
<body>
    <h1>Product List</h1>

    
    <!-- Filter Form -->
    <%- include('filterProducts.ejs') %>

    <ul>
        <% products.forEach(product => { %>
            <li>
                <h2><%= product.name %></h2>
                <p>Price: $<%= product.price %></p>
                <p>Description: <%= product.description %></p>
                <!-- Display the product image -->
                <img src="data:<%= product.image.contentType %>;base64,<%= product.image.data.toString('base64') %>" alt="<%= product.name %>">
                <!-- You can add more product details here -->
                <a href="/order" type="button" class="btn btn-primary btn-lg px-4 me-md-2">Order!</a>
                <a href="/products/vendors-only/my-products/<%= product.id %>/delete" type="button" class="btn btn-danger btn-lg px-4">Delete</a>
            </li>
        <% }); %>
    </ul>
</body>
</html>